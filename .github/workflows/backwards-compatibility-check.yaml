# See https://github.com/Roave/BackwardCompatibilityCheck

name: "Backwards Compatibility Check"

on:
    pull_request: ~

jobs:
    backwards-compatibility-check:
        name: "Backwards Compatibility Check"

        runs-on: "ubuntu-latest"

        # 2.x has no stable release yet, so there is no published API to break and the check has
        # nothing meaningful to compare against - it just reports every deliberate signature change
        # on the way to 2.0.0. Kept running so the report stays visible, but not blocking.
        # Remove this once 2.0.0 is tagged.
        continue-on-error: true

        steps:
            -   name: "Checkout"
                uses: "actions/checkout@v4"
                with:
                    fetch-depth: 0

            -   name: "Setup PHP, with composer and extensions"
                uses: "shivammathur/setup-php@v2"
                with:
                    php-version: "8.1"
                    coverage: "none"

            -   name: "Install tool"
                run: "composer global require roave/backward-compatibility-check"

            -   name: "Check for BC breaks"
                run: "~/.composer/vendor/bin/roave-backward-compatibility-check --from=origin/${{ github.event.pull_request.base.ref }} --format=github-actions"
